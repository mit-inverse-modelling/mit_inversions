[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mit-inversions"
version = "0.1.0"
description = "MIT inversions analysis package"
requires-python = ">=3.8"
dependencies = []

[tool.setuptools]
packages = ["mit_inversions", "mit_inversions.readers"]
